#include "main.h"

/**
 * _strchr - locates char in string
 * @s: the string
 * @c: the character searched
 * Return: Always 0
 */

char *_strchr(char *s, char c)
{
char *res;
res = strchr(s, c);


return (res);
}
