#include "main.h"

/**
 * main - function to print name of program
 * @argc: - count of the cmd aruments
 * @argv: - array of the cmd arguments
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
printf("%d\n", argc);
printf("%s\n", argv[0]);

return (0);
}
