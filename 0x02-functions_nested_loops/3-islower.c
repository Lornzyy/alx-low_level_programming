#include "main.h"
/**
* _islower - function to check if char c is lowercase
* @c: is the int that will be used for this function
* Return :0
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
return (0);
}
