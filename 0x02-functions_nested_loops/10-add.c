#include "main.h"
/**
* add - function to add two integeres
* @num1: the second integer to be used
* @num2: the second integer
*Return: The result from the addition
*/
int add(num1, num2)
{
int result;
result = num1 + num2;
_putchar(result);
return (result);
}
