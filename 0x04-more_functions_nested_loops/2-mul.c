#include "main.h"
/**
 * mul - function to multiply integers
 * @a: first integer to multiply
 * @b: second integer to multiply
 * Return: 0
 */
int mul(int a, int b)
{
int multiply;
multiply = a * b;
return (multiply);
}
