#include "main.h"
/**
* _isupper - function used to find if c is in uppercase
* @c: the integer to be used an argument for this function
* Return: 0
*/
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
return (0);
}
